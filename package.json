{
  "name": "selfie-share",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "install": "cd web-role && npm install && cd ../web-worker && npm install && cd ../test && npm install"
  },
  "author": "",
  "devDependencies": {
  },
  "dependencies": {
  }
}
