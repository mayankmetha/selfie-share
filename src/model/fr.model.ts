export interface Friends {
    peer1: string;
    peer2: string;
    friendshipDate: string;
    friendId: string;
}
export interface frModel {

    frId:string;
    fromUser:string;
    toUser:string;
    dateOfRequest:string;
}
export interface frBody{

    fromUser:string;
    toUser:string;
}